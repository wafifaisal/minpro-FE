import CreateEvent from "@/components/createEvent/createEvent";

export default function CreateEventPage() {
  return (
    <main className="py-20  bg-gradient-to-tl from-blue-500 via-black to-black">
      <CreateEvent />
    </main>
  );
}
